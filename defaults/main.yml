---
galaxy_manage_trackster: yes
galaxy_user_name: galaxy
len_file_path: "{{ galaxy_server_dir }}/config/"
bedtools_version: 2.29.2
galaxy_server_dir: /galaxy/server
galaxy_custom_tools_dir: "{{ galaxy_server_dir }}/.venv//bin"
